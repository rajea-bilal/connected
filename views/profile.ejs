<%- include('partials/header') -%>
<div class="container">
  <div class="row mt-5">
      <div class="col-6">
          <div>
              <p><strong>User Name</strong>: <%= user.userName %></p>
              <p><strong>Email</strong>: <%= user.email %></p>
              <a href="/bio" class="col-3 btn btn-primary">Update Bio</a>
              <a href="/quote" class="col-3 btn btn-primary">Quotes Bank</a>
          </div>


              
      <div>
        <ul class="row list-unstyled">
            <% for(var i=0; i<bio.length; i++) {%>
              <li class="col-6 justify-content-between mt-5">
                
                <p>Current Read: <strong><%= bio[i].currentRead %></strong></p>
              </li>
            <% } %>
            </ul>

            <ul class="row list-unstyled">
                <% for(var i=0; i<bio.length; i++) {%>
                  <li class="col-6 justify-content-between mt-5">
                    <p>Current Project :<strong><%= bio[i].currentProject %></strong></p> 
                   
                  </li>
                <% } %>
                </ul>

                <ul class="row list-unstyled">
                    <% for(var i=0; i<bio.length; i++) {%>
                      <li class="col-6 justify-content-between mt-5">
                        <p>Hobbies: <strong><%= bio[i].hobbies %></strong></p>
                       
                      </li>
                    <% } %>
                    </ul>
        
    </div>
          <div class="mt-5">
            <h2>Share your art</h2>
            <form action="/post/createPost" enctype="multipart/form-data" method="POST">
             
              <div class="mb-3">
                <label for="caption" class="form-label">Caption</label>
                <textarea class="form-control" id="caption" name="caption"></textarea>
              </div>
              <div class="mb-3">
                <label for="imgUpload" class="form-label">Image</label>
                <input type="file" class="form-control" id="imageUpload" name="file">
              </div>
              <button type="submit" class="btn btn-primary" value="Upload">Submit</button>
            </form>
          </div>
      </div>
      
      <div class="col-6">
        <ul class="row list-unstyled">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <a href="/post/<%= posts[i]._id%>">
              <img class="img-fluid" src="<%= posts[i].image%>">
            </a>
          </li>
        <% } %>
        </ul>
        <div class="row justify-content-center mt-5">
          <a class="btn btn-primary" href="/feed">Return to Feed</a>
          <a href="/logout" class="col-12 btn btn-primary btn-logout">Logout</a>
        </div>  
      </div>  
    </div>
  </div>
</div>
<%- include('partials/footer') -%>